apiVersion: v1
kind: ConfigMap
metadata:
    name: {{ .Values.portal.name }}-config
    namespace: {{ .Values.namespace }}
    labels:
        app: {{ .Values.portal.name }}
data:
    appsettings.json: |
        {
          "ApiBaseUrl": "https://api.{{ .Values.ingress.domain }}",
          "Assets": {
            "MaxUploadSizeInBytes": {{ .Values.portal.assets.maxUploadSizeInBytes }}
          },
          "Oidc": {
            "Authority": "https://authentication.{{ .Values.ingress.domain }}/realms/{{ .Values.keycloak.realm }}",
            "ClientId": "{{ .Values.portal.oidc.clientId }}",
            "ResponseType": "code",
            "DefaultScopes": [
              "openid",
              "profile",
              "email"
            ]
          }
        }    
    security.txt: |
        Contact: {{ .Values.securityTxt.contact }}
        Expires: {{ .Values.securityTxt.expires }}
        Preferred-Languages: {{ .Values.securityTxt.preferredLanguages }}
        Canonical: https://fileshare.{{ .Values.ingress.domain }}/.well-known/security.txt
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: {{ .Values.portal.name }}
    namespace: {{ .Values.namespace }}
    labels:
        app: {{ .Values.portal.name }}
spec:
    replicas: {{ .Values.portal.replicas }}
    selector:
        matchLabels:
            app: {{ .Values.portal.name }}
    template:
        metadata:
            labels:
                app: {{ .Values.portal.name }}
        spec:
            automountServiceAccountToken: false
            containers:
                - name: {{ .Values.portal.name }}
                  image: "{{ .Values.portal.image.repository }}:{{ .Values.appVersion }}"
                  imagePullPolicy: {{ .Values.imagePullPolicy }}
                  ports:
                      - containerPort: {{ .Values.portal.port }}
                  resources:
                      requests:
                          cpu: {{ .Values.portal.resources.requests.cpu }}
                          memory: {{ .Values.portal.resources.requests.memory }}
                          ephemeral-storage: {{ index .Values.portal.resources.requests "ephemeral-storage" }}
                      limits:
                          cpu: {{ .Values.portal.resources.limits.cpu }}
                          memory: {{ .Values.portal.resources.limits.memory }}
                          ephemeral-storage: {{ index .Values.portal.resources.limits "ephemeral-storage" }}
                  volumeMounts:
                      - name: appsettings
                        mountPath: /usr/share/nginx/html/appsettings.json
                        subPath: appsettings.json
                        readOnly: true
                      - name: appsettings
                        mountPath: /usr/share/nginx/html/.well-known/security.txt
                        subPath: security.txt
                        readOnly: true
                  livenessProbe:
                      httpGet:
                          path: /health.txt
                          port: {{ .Values.portal.port }}
                      initialDelaySeconds: 5
                      periodSeconds: 10
                  readinessProbe:
                      httpGet:
                          path: /health.txt
                          port: {{ .Values.portal.port }}
                      initialDelaySeconds: 5
                      periodSeconds: 10
            volumes:
                - name: appsettings
                  configMap:
                      name: {{ .Values.portal.name }}-config
---
apiVersion: v1
kind: Service
metadata:
    name: {{ .Values.portal.name }}
    namespace: {{ .Values.namespace }}
    labels:
        app: {{ .Values.portal.name }}
spec:
    selector:
        app: {{ .Values.portal.name }}
    ports:
        - port: {{ .Values.portal.port }}
          targetPort: {{ .Values.portal.port }}
          protocol: TCP
