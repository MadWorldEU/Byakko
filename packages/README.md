# packages

This folder contains the local NuGet package cache used by this solution. Packages are restored here automatically by the .NET toolchain based on dependencies defined in the project files and versioned centrally in `Directory.Packages.props`.

Do not manually add or modify files in this folder.